Overview

This project implements a terminal-based Sudoku game in C++ that allows users to play and solve a 9×9 Sudoku puzzle directly from the command line. The program manages the Sudoku grid, validates player inputs, and ensures that all moves follow standard Sudoku rules.
